# Nesta etapa o app roda inteiramente com dados mockados (src/data/mockFernanda.ts).
# Estas variáveis só passam a ser necessárias quando o Supabase for conectado de fato.
# Copie este arquivo para .env.local e preencha com os dados do seu projeto Supabase
# (Project Settings > API no dashboard do Supabase).
NEXT_PUBLIC_SUPABASE_URL=https://SEU-PROJETO.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key-publica
